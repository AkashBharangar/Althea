# Local dev: leave unset — Vite proxies /api to the backend (see vite.config.js).

# Production / preview on Vercel: public URL of your Render API (no trailing slash).
# Example:
# VITE_API_URL=https://althea-api.onrender.com
